# Uncomment this to enable some debug logging
# DEBUG=1

# Okta Configuration (Optional - omit for single-auth profile)
# Provide your Okta endpoint and credentials here if using OAuth2 authentication.
# If these values are missing, API tests will use the 'single-auth' profile automatically.
# This allows running API tests without an OAuth2 provider setup.
OKTA_URL=https://dev-XXXXXXXX.okta.com
OKTA_USERNAME=XXX.XXX@example.com
OKTA_PASSWORD=XXX

# Admin Auth Configuration (Optional - only used with single-auth profile)
# These variables are used when running API tests with the 'single-auth' profile
#TEST_ROLE=ROLE_Osmt_Admin
#TEST_USER_NAME=test-user
#TEST_USER_EMAIL=test@example.com
